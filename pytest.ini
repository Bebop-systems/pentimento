[pytest]
testpaths = tests
pythonpath = . src
# Warnings are errors on purpose: this gate caught two real file-handle
# leaks (ExifTool's pipes, and werkzeug's spooled upload) that would
# otherwise have shipped silently.
filterwarnings = error
